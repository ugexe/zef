#!/usr/bin/env perl6
use Zef;
use Zef::App;




multi sub MAIN('install', *@install_us, Bool :$verbose = False, Bool :$notest = False) {
    say "test: $test";

    my $zef = Zef::App.new;
    $zef.install(:$notest, @install_us);
}